# Supabase Configuration
SUPABASE_URL=http://localhost:8000
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Crawler Settings
CRAWLER_TIMEOUT=60000

# API Server
API_HOST=0.0.0.0
API_PORT=8001

# Webhook callback URL (Next.js app)
NEXTJS_WEBHOOK_URL=http://localhost:3000/api/webhook/crawl
CRAWLER_WAIT_UNTIL=load

# NOTE: LLM and adaptive crawling settings are configured in the database
# system_settings table (key: 'crawler_config'). See migration 012_crawler_config.sql
